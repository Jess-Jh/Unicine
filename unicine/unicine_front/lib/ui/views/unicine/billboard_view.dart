import 'package:flutter/material.dart';

class BillboardView extends StatelessWidget {
  const BillboardView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Vista peliculas'),
    );
  }
}
